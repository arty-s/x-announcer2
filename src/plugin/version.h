// One place for the version string, so the log, the panel and a report all name
// the same build. 1.x is versioned separately and is at 1.1.5.
#pragma once

namespace xa {

constexpr const char* kPluginVersion = "2.0.0-dev";

}  // namespace xa
